## What this changes

<!-- One or two sentences. -->

## Why

<!-- The problem it solves. If it fixes a bug, describe the failure. -->

## Checklist

- [ ] `python tests/test_offline.py` passes
- [ ] If this fixes a bug, I added the test that would have caught it
- [ ] No module is named after a Python standard-library module
- [ ] Nothing writes to stdout (stdio MCP uses stdout as the protocol channel)
- [ ] Any new source is a public API whose terms permit programmatic use, and
      its terms URL is recorded in the module docstring
